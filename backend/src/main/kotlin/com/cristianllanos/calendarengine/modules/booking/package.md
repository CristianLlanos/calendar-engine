# Package com.cristianllanos.calendarengine.modules.booking

Booking and booking URL management services.

[BookingService] handles CRUD for bookings scoped to a tenant. [BookingUrlService] manages shareable booking links
with configurable availability windows, durations, buffers, and limits.
