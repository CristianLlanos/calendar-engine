# Package com.cristianllanos.calendarengine.config

Configuration singletons for database, sync providers, and event emitters.

In standalone mode these read from `application.conf`. In plugin mode they are set by the `CalendarEngineConfig` DSL.
