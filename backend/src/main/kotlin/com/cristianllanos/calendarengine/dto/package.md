# Package com.cristianllanos.calendarengine.dto

Data transfer objects for API requests and responses.

Includes serializable request/response classes for tenants, calendars, events, bookings, booking URLs, and availability.
Also provides shared helpers for pagination, CRUD route generation, and error responses.
