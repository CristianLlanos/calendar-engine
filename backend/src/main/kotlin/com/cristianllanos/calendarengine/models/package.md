# Package com.cristianllanos.calendarengine.models

JetBrains Exposed table definitions for all database entities.

Each object extends `Table` and defines columns, primary keys, and foreign key references.
Tables are auto-created on startup when `createTables` is enabled.
