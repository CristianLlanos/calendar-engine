# Package com.cristianllanos.calendarengine.models.enums

Enum classes representing status values and configuration options stored in the database.

Includes booking status, event status, calendar visibility, sync mode, and sync provider enums.
